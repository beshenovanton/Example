import NeuroNetLibrary

nv.say(hello, val = None)

nv.listen()

r = nlu.extract('Да я хочу cashback', 'increase_cashback')

                      ## Не смог разобраться до конца как работает логака библиотеки NeuroNet
